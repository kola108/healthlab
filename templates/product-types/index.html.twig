{% extends 'base.html.twig' %}

{% block title %}Discount{% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>

    <div class="example-wrapper">

        {% if is_granted('ROLE_ADMIN') %}
            <a href="{{ path('product_type_add') }}">Add item</a>
        {% endif %}

        {% for item in list  %}
            <h2>{{ item.name }}</h2>

            {% if is_granted('ROLE_ADMIN') %}
                <a href="{{ path('product_type_delete', {'productTypeId': item.id}) }}">Delete item</a>
                <a href="{{ path('product_type_edit', {'productTypeId': item.id}) }}">Edit item</a>
            {% endif %}

        {% endfor %}
    </div>
{% endblock %}
